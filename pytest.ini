[pytest]
# 路径与发现
testpaths = tests
addopts = -ra -q --strict-markers --tb=short

# pytest-qt：项目用 PyQt5，强制指定避免被 PyQt6 抢走
qt_api = pyqt5

# markers：方便用 -m "not integration" 跳过集成
markers =
    integration: integration tests that mock HTTP / drive QThread
